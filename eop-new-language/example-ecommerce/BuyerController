controller BuyerController extends UserController {

    entity BuyerEntity
    OrderEntity order

    OrderEntity buy() {
        if (true) {
            OrderEntity order = new OrderEntity:OrderController(entity)
            return order
        } else {
            error()
        }
    }
        
    private void error() {
        
    }

}
